<?php

	namespace model;

	use think\Model;

	class Banner extends Model
	{


	  	protected $table = 'lx_banner';
	  	

		// protected static function init()
		// {
		// 	parent::init();
		// 	if( !self::$user_id)  session('user_info.user_id') ? self::$user_id = session('user_info.user_id') : '2'; 
		// }

	}
